table! {
    hero (id) {
        id -> Integer,
        name -> Varchar,
        identity -> Varchar,
        hometown -> Varchar,
        age -> Integer,
        image_url -> Nullable<Text>,
    }
}
